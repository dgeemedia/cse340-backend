<!-- views/reviews/_reply.ejs -->
<%- include('reviews/_reply', { reply }) %>

<div class="reply-item" data-reply-id="<%= reply.reply_id %>" style="margin-bottom:8px; padding-left:12px; border-left:2px solid rgba(15,23,42,0.04);">
  <div class="reply-meta" style="font-size:0.9rem; color:var(--muted);">
    <strong><%= reply.account_firstname || 'Staff' %> <%= reply.account_lastname || '' %></strong>
    <span style="margin-left:8px;">— <%= new Date(reply.created_at).toLocaleString() %></span>
  </div>
  <div class="reply-text" style="margin-top:6px;"><%= reply.reply_text %></div>
</div>
